name: mlflow-project

# Gunakan file requirements.txt
pip_requirements: requirements.txt

entry_points:
  main:
    parameters:
      C: {type: float, default: 1.0}
    command: >
      python modelling.py --C {C}
